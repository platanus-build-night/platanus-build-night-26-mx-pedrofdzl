[project]
name = "ditto-backend"
version = "0.1.0"
description = "Ditto — autonomous compliance agent (backend)"
requires-python = ">=3.11"
dependencies = [
    "django>=5.1,<5.3",
    "djangorestframework>=3.15",
]

[dependency-groups]
dev = [
    "ruff>=0.8",
]
